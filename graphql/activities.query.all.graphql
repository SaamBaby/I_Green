query GetAllActivities {
    activities {
        activity_id
        is_accepted
        is_completed
        shift_endtime
        shift_id
        shift_starttime
        shift {
            shift_date
            shift_name
            shift_notes
            shift_time
            job {
                job_location
                job_name
            }
        }
    }
}