mutation UpdateActivity($id: String!, $starttime: String,$endtime: String ) {
    update_activities(where: {activity_id: {_eq:$id}},
        _set: {is_completed: true, shift_starttime:$starttime, shift_endtime:$endtime}) {
        returning {
            shift_endtime
            shift_starttime
        }
    }
}