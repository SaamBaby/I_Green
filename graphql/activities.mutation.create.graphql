mutation CreateActivity($activityId: Int!,$shiftId: Int!,$userId: String!,$isAccepted:Boolean) {
    insert_activities_one(object: {activity_id: $activityId, is_accepted: $isAccepted, shift_id: $shiftId, user_id: $userId}) {
        activity_id
    }
}
